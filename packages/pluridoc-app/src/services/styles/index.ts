import {
    StyledButton,
    StyledLinkButton,
} from './buttons';

import {
    StyledSwitch,
    StyledInputText,
} from './inputs';


export default {
    Button: StyledButton,
    LinkButton: StyledLinkButton,
    Switch: StyledSwitch,
    InputText: StyledInputText,
}
